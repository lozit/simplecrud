{% extends 'base.html.twig' %}

{% block title %}Ma super Bibliothèque{% endblock %}

{% block body %}
<div class="container text-center">
  <div class="row">
    <table class="table">
      <thead>
        <tr>
          <th>Titre</th>
          <th>Nb pages</th>
          <th>Genre</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {% for livre in livres %}
        <tr>
            <td><a href="/livre/{{ livre.id }}">{{ livre.titre }}</a></td>
            <td>{{ livre.nbpages }}</td>
            <td>{{ livre.genre }}</td>
            <td><a href="/editer/{{ livre.id }}">Editer</a> | <a href="/supprimer/{{ livre.id }}">Supprimer</a></td>
        </tr>
        {% endfor %}
      </tbody>
    </table> 
  </div>
</div>
{% endblock %}
